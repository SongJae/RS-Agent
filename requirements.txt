# ── Core (항상 필요) ──────────────────────────────
numpy>=1.24.0
scikit-learn>=1.3.0
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2
Pillow>=10.0.0
PyYAML>=6.0
gradio>=4.0.0
rank-bm25>=0.2.2
rich>=13.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── LLM 백엔드 (하나 이상 설치) ─────────────────────
# Anthropic Claude API (외부망 환경)
anthropic>=0.40.0

# OpenAI 호환 로컬 LLM (폐쇄망 환경 — vLLM / Ollama / LM Studio)
openai>=1.0.0

# ── 선택 사항 ────────────────────────────────────
tiktoken>=0.5.0
