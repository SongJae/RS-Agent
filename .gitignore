# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Virtual environments
.venv/
venv/
env/

# Environment variables
.env

# Distribution / packaging
dist/
build/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# Models (대형 LLM 모델 — 100MB 초과 파일 제외)
# 임베딩 모델(all-MiniLM-L6-v2, 87MB)은 포함됨
models/*/pytorch_model-*.bin
models/*/model-*.safetensors

# Miniconda 설치 원본 (155MB — 분할 파일인 *.partaa/ab 로 대체)
install/Miniconda3-latest-Linux-x86_64.sh

# Data outputs
*.log

# conda 환경 패키지 (3GB — USB로 별도 복사)
rs-agent-env.tar.gz

# 런타임 생성 실행 스크립트 (install_offline.sh가 자동 생성)
run_server.sh
run_server_gpu.sh
run_ui.sh

# 대용량 위성 이미지 (GitHub 100MB 제한)
data/images/landsat9.tif
data/images/rgb_satellite.tif
data/images/geotiff_test/
data/images/geotiff_test.zip
data/images/small.tif
data/images/float32.tif
data/images/alpha.tif
